name: Deploy
on:
  push:
    branches:
      - master

jobs:
  deploy:
    uses: abatilo/workflows/.github/workflows/ci.yml@main
    with:
      name: blog
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}

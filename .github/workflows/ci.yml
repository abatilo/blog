name: Deploy
on:
  push:
    branches:
      - master

jobs:
  deploy:
    uses: abatilo/workflows/.github/workflows/ci.yml@86657301ec2b16a04e4ccc8b624fe093d2a9b79d
    with:
      name: blog
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
